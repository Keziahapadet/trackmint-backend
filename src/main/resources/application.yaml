spring:
  config:
    activate:
      on-profile: "!local"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:keziahapadet41@gmail.com}
    password: ${MAIL_PASSWORD:qovm thpp toje gkda}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true


            resend:
              api:
                key: re_8qMyTv53_Nphkcr1xQ3Qr4TxJrCpuF6rT


springdoc:
  swagger-ui:
    enabled: true
  api-docs:
    enabled: true

server:
  port: ${PORT:8080}

app:
  jwt:
    secret: ${JWT_SECRET:trackmintSecretKeyTrackmintSecretKey123456}
    expiration: ${JWT_EXPIRATION:86400000}
  frontend:
    url: ${FRONTEND_URL:http://localhost:4200}

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    com.trackmint.app: DEBUG